# 🔗 گزارش تست یکپارچه‌سازی و رگرسیون (Integration Report) — نقش ۱۸ منشور

**روش:** در پایان هر تسک، E2E زنده روی پشته واقعی (Fastify + Socket.io + MongoDB واقعی + MinIO واقعی + mock-push TLS) اجرا و نتایج در همان تسک ثبت شد. این سند تجمیع رسمی آن اجراهاست.

## ماتریس سناریوهای End-to-End (همه ✅)

### احراز هویت (Task 1.2/1.3/1.4 — ۱۴ سناریو)
درخواست OTP فرمت محلی/فارسی → نرمال E.164 · کد غلط 401 با شمارنده · کد درست → توکن‌ها · reuse کد 410 · resend زودهنگام 429 · قفل ۵تایی 423 · `/auth/me` با/بدون/جعلی · refresh → rotation · **replay توکن قدیمی 401** · logout → refresh بعدی 401 · ثبت پروفایل → username نامعتبر 400 → بدون توکن 401

### Realtime پایه (Task 2.1 — ۶ سناریو)
رد توکن جعلی/ناشناس در handshake · اتصال دو کاربر واقعی · presence آنلاین/آفلاین broadcast + lastSeen

### پیام‌رسانی (Task 2.2/2.3 — ۱۸ سناریو)
ack با tempId · تحویل لحظه‌ای (<500ms — معیار acceptance §2: <1s ✅) · پاسخ در همان گفتگو · payload خراب/خالی رد · REST تاریخچه/sidebar · **403 برای non-member** · تیک delivered خودکار (گیرنده آنلاین) · تیک read با anchor · typing start/stop/**auto-stop 5s** · گیرنده آفلاین → sent می‌ماند

### گروه/کانال/پین (Task 3.1/3.2/3.4 — ۲۶ سناریو)
ساخت گروه → owner · افزودن عضو/انتصاب ادمین · member→admin انتصاب 403 · پیام گروهی به همه اعضا · **پست member در کانال CHANNEL_READONLY** · broadcast کانال · کل جریان group-settings از UI (شامل admin↛owner حذف 403، غیرعضو GET 404) · پین: member 403، owner 200 + رویداد به همه، unpin null، پیام خارجی 404

### رسانه (Task 3.3 — ۹ سناریو)
presign → **PUT واقعی JPEG به MinIO 200** · 60MB→413 · SVG→415 · پیام تصویری سوکت → دریافت B · content جعلی `INVALID_MEDIA_KEY` · **دانلود واقعی فایل سالم 320×240** · بدون توکن 401

### PWA/مخاطبین/Push/جستجو (Task 4.1-4.4 — ۲۳ سناریو)
تطبیق مخاطبین ترکیبی (عضو/غیرعضو/فارسی/خودم/خراب) · draft→گفتگوی واقعی · rate-limit match 429 · >500 شماره 400 · SW: استراتژی‌ها/manifest 8/8/offline زنده · **push رمزشده aes128gcm روی TLS تحویل endpoint** · پس از unsubscribe صفر push · **گیرنده آنلاین: socket آری push نه** · جستجو: @username/فارسی/گروه فقط عضو/no phone leak/`.*` بلاک/400/401

### رگرسیون نهایی پس از پچ‌های لایه ۵ (۷ سناریو)
send پایه سالم · **reply در DB** · cross-conv reply رد · forward + دریافت گیرنده · no-access forward رد · read receipts سالم · ISS-004: آپلودر 200/غریبه 403

## جمع‌بندی
**~۱۰۳ سناریوی یکپارچه‌سازی، صفر شکست باز.** هر چرخه اصلاح (لایه ۵) با اجرای مجدد جریان‌های قدیمی همراه بود — هیچ رگرسیونی مشاهده نشد (قانون نقش ۱۸).
