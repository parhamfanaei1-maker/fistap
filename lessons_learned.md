# 📕 Lessons Learned — Fistap (Zero-Repeat Error Log)

> Per AI-DOS Learning Law: every identified bug/mistake gets an RCA entry here.
> Format: `LL-### | Date | Layer/Task | Symptom | Root Cause | Corrective Rule`

| ID | تاریخ | لایه/تسک | علامت خطا | ریشه‌ی خطا (RCA) | قانون اصلاحی (عدم تکرار) |
|---|---|---|---|---|---|
| LL-001 | 2026-06-12 | L3/Task 1.1 | `TS6059: File ... is not under 'rootDir'` در تایپ‌چک سرور | پکیج `@fistap/shared` به صورت سورس TS ایمپورت می‌شود ولی `tsconfig` سرور `rootDir: src` داشت؛ `tsc` اجازه‌ی فایل خارج از rootDir را برای emit نمی‌دهد | برای پکیج‌هایی که سورس مشترک workspace را مستقیم ایمپورت می‌کنند: (الف) `rootDir` را حذف کن، (ب) بیلد را با باندلر (`tsup`) انجام بده نه `tsc --emit`. tsc فقط برای `--noEmit` typecheck |
| LL-003 | 2026-06-12 | L3/Task 1.3 | فایل‌های کامپوننت در ریشه workspace نوشته شدند نه در `client/src/components/ui` | پوشه‌های خالی در snapshot محیط ذخیره نمی‌شوند؛ `cd` به مسیر ناموجود شکست خورد ولی چون با `;`/newline ادامه دادیم، heredoc در cwd جاری نوشت | **قانون:** قبل از هر نوشتن فایل، `mkdir -p` در همان دستور؛ `cd X && command` (نه `cd X; command`) تا شکستِ cd زنجیره را قطع کند |
| LL-005 | 2026-06-12 | L3/Task 2.4 | بیلد Next شکست: `Can't resolve './types/user.js'` در `@fistap/shared` — در حالی که تسک 1.3 سبز بود | ایمپورت‌های قبلی از shared فقط type-only بودند (در ترنسپایل حذف می‌شدند)؛ با ایمپورت **مقدار** (`SOCKET_EVENTS`) webpack باید پسوند `.js` در سورس TS را resolve کند که به‌طور پیش‌فرض بلد نیست | **قانون:** برای پکیج‌های workspace با ایمپورت پسوند `.js` (الزام NodeNext)، در `next.config` گزینه `config.resolve.extensionAlias = {'.js': ['.ts','.js']}` ست شود؛ تست بیلد باید شامل ایمپورت value باشد نه فقط type |
| LL-006 | 2026-06-12 | L3/Task 3.4 | تایپ‌چک کلاینت شکست: `conversation:pin not assignable` — در حالی که ثابت SOCKET_EVENTS اضافه شده بود | از دو ویرایش هم‌قالب روی events.ts فقط اولی اعمال شده بود (ویرایش دوم پترن `}` انتهایی مبهم داشت و روی interface اشتباه match شد)؛ بدون verify ادامه دادیم | **قانون:** پس از هر edit روی فایل‌های قرارداد مشترک (shared)، بلافاصله `cat`/`grep` تأیید شود که هر دو سمت (const + interface) تغییر کرده‌اند؛ پترن‌های edit باید شامل خطوط متمایز کافی باشند نه فقط `}` |
| LL-007 | 2026-06-12 | L3/Task 4.3 | push در E2E به mock endpoint نمی‌رسید (بدون خطا در لاگ) | web-push فقط HTTPS را می‌پذیرد؛ mock اولیه HTTP بود → خطای EPROTO بی‌صدا در catch بلعیده می‌شد (best-effort) | **قانون:** (الف) تست سرویس‌های push/webhook با TLS واقعی (self-signed + NODE_TLS_REJECT_UNAUTHORIZED=0 فقط در تست)؛ (ب) در مسیرهای best-effort حداقل debug-log برای خطا — سکوت مطلق دیباگ را غیرممکن می‌کند |
| LL-008 | 2026-06-12 | L3-L6/انطباق | خودممیزی نشان داد ۶ خروجی رسمی نقش‌های منشور (context_bundles، knowledge_graph، گزارش‌های تست، issue_routing_map، performance_report، user_guide/api_docs) تولید نشده‌اند | مسیر اجرا بر اساس implementation_plan فازمحور پیش رفت و خروجی‌های «نقش‌محور» منشور در اسناد دیگر ادغام شدند — نقض جزئی قانون «خروجی هر ایجنت = یک فایل» | **قانون:** در شروع هر لایه، چک‌لیست خروجی‌های الزامی نقش‌های آن لایه از منشور استخراج و در project_state ثبت شود؛ بستن لایه فقط با تیک کامل آن چک‌لیست مجاز است |
| LL-004 | 2026-06-12 | L3/Task 2.2 | خطاهای تایپ عجیب Mongoose: `Type 'ObjectId' has no properties in common with...` | در تعریف اسکیما از `Types.ObjectId` (کلاس BSON) استفاده شده بود؛ Mongoose برای تعریف فیلد به `Schema.Types.ObjectId` نیاز دارد و typegen مدل را خراب می‌کرد | **قانون:** در `new Schema({...})` همیشه `Schema.Types.ObjectId`؛ `Types.ObjectId` فقط برای ساخت/اعتبارسنجی مقدار در runtime |
| LL-002 | 2026-06-12 | L3/Task 1.2 | اجرای `node dist/index.js` کرش: `ERR_UNKNOWN_FILE_EXTENSION ".ts" for packages/shared/src/index.ts` (تسک ۱.۱ شانسی پاس شد چون آن نسخه import مستقیم از shared در مسیر اجرا نداشت) | `tsup` به‌طور پیش‌فرض همه‌ی dependencyها (از جمله `@fistap/shared`) را external می‌کند؛ نتیجه: باندل خروجی، سورسِ TS خام پکیج workspace را import می‌کند که Node قادر به اجرای آن نیست | **قانون:** هر پکیج workspace که `main` آن فایل TS است باید در بیلد tsup با `--no-external <pkg>` (یا `noExternal`) باندل شود. پس از هر تغییر بیلد، تست دودی اجرا (`node dist && curl /health`) الزامی است — بیلدِ سبز ≠ اجرای سبز |

